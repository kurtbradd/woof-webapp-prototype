The Woof Backend & Web App

Requirements:
Redis
MongoDB

To Run:
Install NPM Dependencies
npm install

sudo mongod (starts mongo)
redis-server (starts redis)
node worker.js (from worker directory)
node app.js (starts the app)
